# Proyectos correspondientes al cápitulo 8 
Reactive Programming with Project Reactor and Spring WebFlux.

**Notas:** 
* Para este capítulo al igual que en capítulos anteriores nos brinda una "base" de proyectos que usaremos a lo largo del capítulo, en esta ocación nos da una proyecto base tanto de `planefinder` como de de `aircraft-positions`. Y para evitar problemas, usaremos esos proyectos base, lo que implica que trabajaremos con Spring Boot 2.

* Este README se modifica tras avanzar durante el capítulo.

## planefinder

Este es un proyecto inicial que no desarrollamos, que hemos usado igualmente en capítulos anteriores.
Como tal de lo que se encarga este proyecto es de generar objetos "Aircraft" y exponerlos en el endpoint 
GET /aircraft


## aircraft-positions

Este es el proyecto que desarrollamos a la par del libro.
